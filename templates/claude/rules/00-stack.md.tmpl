---
id: rule-00-stack
status: managed
populated_by: apex-installer
applies_to: all
last_updated: {{INSTALL_DATE}}
apex_version: {{APEX_VERSION}}
---

# Stack

Auto-generated by APEX from repo signals. Re-rewritten on every `apex upgrade`. Edit `10-conventions.md` or `20-gotchas.md` to override anything here, or add a higher-numbered rule file (`30-*.md`, etc.) for project-specific guidance.

- **Language:** {{STACK_LANGUAGE}}
- **Frameworks:** {{STACK_FRAMEWORKS}}
- **Package manager:** {{STACK_PACKAGE_MANAGER}}
- **Test runner:** {{STACK_TEST_RUNNER}}
- **Lint:** {{STACK_LINT}}
- **Format:** {{STACK_FORMAT}}
- **CI:** {{STACK_CI}}

## Common Commands

{{COMMON_COMMANDS}}

## Notes

- Treat the package manager and test runner above as authoritative. Do not substitute equivalents from other ecosystems.
- If detection got something wrong, run `apex init --force` (after correcting `package.json` / lockfile signals) or override locally in `.claude/rules/10-conventions.md`.
